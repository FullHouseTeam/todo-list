name: CD_PUSH

on:
  push:
    branches:
      - '**'

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node_version: [18]
      fail-fast: false

    name: Lint, Test, and Check Formatting
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 1

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node_version }}

      - name: Install Dependencies
        run: npm install --no-frozen-lockfile

      - name: Test Unit
        run: npm run test:unit

      - name: Lint
        run: npm run lint

      - name: Check Formatting
        run: npm run format:check

  check_commits_convetion:
    name: Conventional Commits Message
    runs-on: ubuntu-latest
    steps:
      - uses: gsactions/commit-message-checker@v2
        with:
          pattern: '(?:feat|fix|chore|test|docs|build|ci|style|refactor|perf|revert)(?:\(\w+\))?:\s[a-z]{1,2}.+'
          error: 'The commit message must have the following structure: <type>(?): <description>'
          excludeDescription: 'true'
          excludeTitle: 'true'
          checkAllCommitMessages: 'true'
          accessToken: ${{ secrets.GITHUB_TOKEN }}

  check_branches_convention:
    name: Conventional Branches Name
    runs-on: ubuntu-latest
    steps:
      - uses: deepakputhraya/action-branch-name@master
        with:
          regex: '([a-z]+)/([a-z]+)'
          allowed_prefixes: 'jlteran,resprella,bsoliz,lespinoza,horopeza,jtancara,feature,stable,fix,chore,docs,feat,build,ci,test,refactor,perf,hotfix'
          ignore: main,develop
          min_length: 4
          max_length: 100
